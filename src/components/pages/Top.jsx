import styled from "styled-components";

export const Top = () => {
  return (
    <SContainer>
      <h2> Top page death! </h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
